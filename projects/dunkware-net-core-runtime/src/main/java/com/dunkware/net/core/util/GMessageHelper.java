package com.dunkware.net.core.util;

public class GMessageHelper {
	
	/*
	 * public static LocalDateTime getDateTime(GMessage message, String key) throws
	 * DException { GProperty prop = getProperty(message, key); Timestamp tm =
	 * prop.getValue().getDateTime().getValue(); return DunkTime.toLocalDateTime(tm,
	 * DTimeZone.NewYork); }
	 * 
	 * public static GProperty getProperty(GMessage message, String key) throws
	 * DException { for (GProperty prop : message.getPropertiesList()) {
	 * if(prop.getName().equals(key)) { return prop; } } throw new
	 * DException("Property " + key + " not found on GBean"); }
	 */

}

