package com.dunkware.trade.service.data.service.stream.container.connection.scanner;

import com.dunkware.net.proto.netstream.GNetEntityScannerUpdate;

public class EntityScanner {
	
	// you start this at this point
	// responsible for returning exception message
	// or we send messages to all workers, hookup as a listener to the server message bus
	// everytime we get a scanner update for us we send it back as a 
	///GNetEntityScannerUpdate
	

}
