accountSummary

only 2 open active subscriptions at a time 

 IBApi.EClient.reqAccountUpdates.
  updateAcccountValue() is callbakc 
  
  
request positions 

        client.reqPositions();
		client.cancelPositions()
		
 
 order status  callbacks are 
 openOrder
 orderStatus
         client.reqOpenOrders();
           - this gets callback openOrder and orderStatus 
           openOrderEnd is when you know you have all the open orders 
           
         client.reqAutoOpenOrders(true);
 
 client id 
 