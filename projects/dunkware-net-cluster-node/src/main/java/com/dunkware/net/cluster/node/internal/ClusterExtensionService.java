package com.dunkware.net.cluster.node.internal;

import org.springframework.stereotype.Service;

@Service
public class ClusterExtensionService {
	
	// listen for a cluster event message - and invoke listeners 
	// listen for a cluster service request message and send response
	// listen for a cluster service response message -> and bring back. 

}
