package com.dunkware.trade.service.data.worker.stream.net.container;

import com.dunkware.xstream.net.core.container.Container;

public class WorkerContainer {

	private Container streamContainer;
	
	public void start(WorkerContainerInput input) throws Exception {
		
		// so here we need to figure out how we consume the fuckin messages; 
		// right? 
		
	}
}
