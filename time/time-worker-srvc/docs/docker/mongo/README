To Start:
docker-compose up -d 

To Stop
docker-compose down

Mongo Express Web URL
http://localhost:8081/


NOTE* Line 11 Needs to be changed to point to a location on file system
which will store mongodb data so it can be persisted across multiple docker runs